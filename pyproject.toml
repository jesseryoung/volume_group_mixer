[project]
name = "volume-group-mixer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamcontroller-plugin-tools>=2.0.1",
    "loguru",
    "pulsectl",
]

[tool.uv]
package = false
